<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/txtLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="100dp"
        android:text="Hello World!"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnShowString"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="@string/showString"
        android:textAllCaps="false"
        app:layout_constraintEnd_toEndOf="@+id/txtLabel"
        app:layout_constraintStart_toStartOf="@+id/txtLabel"
        app:layout_constraintTop_toBottomOf="@+id/txtLabel" />

    <Button
        android:id="@+id/btnShowJSON"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="@string/showJSON"
        android:textAllCaps="false"
        app:layout_constraintEnd_toEndOf="@+id/txtLabel"
        app:layout_constraintStart_toStartOf="@+id/txtLabel"
        app:layout_constraintTop_toBottomOf="@+id/btnShowString" />

</androidx.constraintlayout.widget.ConstraintLayout>